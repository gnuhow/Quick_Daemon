# -*- mode: python -*-
a = Analysis(['quick_daemon_iterated.py'],
             pathex=['C:\\Users\\user\\Documents\\GitHub\\Quick_Daemon'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='quick_daemon_iterated.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='quick_daemon_iterated')
